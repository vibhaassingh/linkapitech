/** Scene colors — mirrors the CSS tokens in globals.css (:root). */
export const PALETTE = {
  navy900: 0x0a1f44,
  navy600: 0x1e4a94,
  steel: 0x8fa1bc,
  steel2: 0xc3cddc,
  ink: 0x0b1b33,
  /** Cursor-reactive violet accent (--violet). Used only for motion accents. */
  violet: 0x6d5ae6,
} as const;
